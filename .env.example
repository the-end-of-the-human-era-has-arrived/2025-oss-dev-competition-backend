# Environment Variables Configuration Example
# ===========================================
# This file serves as a template for environment variables used by the mindmap server.
# Copy this file to '.env' and update the values according to your environment.
# 
# Usage:
# - Development: Copy to .env and modify values for local development
# - Production: Set these as actual environment variables in your deployment
# - Docker: Use docker-compose.yml or Kubernetes ConfigMap/Secrets
#
# Note: Never commit actual secrets or sensitive data to version control!

# ===========================================
# OAuth Configuration (Required)
# ===========================================
# OAuth settings for Notion API integration
# These are required for authentication with Notion services

# OAuth Client ID from your Notion integration
OAUTH_CLIENT_ID=your_notion_oauth_client_id_here

# OAuth Client Secret from your Notion integration (SENSITIVE - Keep secure!)
OAUTH_CLIENT_SECRET=your_notion_oauth_client_secret_here

# OAuth Redirect URL for authentication callback
# Should match the URL configured in your Notion integration settings
OAUTH_REDIRECT_URL=http://localhost:8080/auth/callback

# ===========================================
# Database Configuration (Optional - if using external DB)
# ===========================================
# Database connection settings
# Currently using in-memory storage, but ready for database integration

# Database host address
DB_HOST=localhost

# Database port
DB_PORT=5432

# Database username
DB_USER=mindmap_user

# Database password (SENSITIVE - Keep secure!)
DB_PASSWORD=your_secure_database_password_here

# Database name
DB_NAME=mindmap_db

# SSL Mode for database connection (disable, require, verify-ca, verify-full)
DB_SSL_MODE=disable

# Database timezone setting
DB_TIME_ZONE=UTC
